$(".flash-container").html("<%= j (render partial: "layouts/flash") %>")

$(".new-container").empty();
$(".new-container").html("<%= j (render partial: "show", locals: { question: @question, choices: @choices, recommendation: @recommendation }) %>");
$(document).foundation("slider", "reflow");
